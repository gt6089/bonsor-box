<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <%= link_to "BonsorBox", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <% if logged_in? %>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active"><%= link_to "Dashboard", dashboard_path, class: "nav-link" %></li>
      <li class="nav-item"><%= link_to "Profile", edit_player_path(current_user), class: "nav-link" %></li>
      <li class="nav-item"><%= link_to "Players", players_path, class: "nav-link" %></li>
    </ul>
    <div class="col-auto navbar-text">
      Your status: <%= status(current_user) %>
    </div>
    <div class="col-auto">
      <%= link_to "Logout", logout_path, method: :delete, class: "btn btn-block btn-secondary" %>
    </div>
    <% end %>
  </div>
</nav>