<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <%= link_to "BonsorBox", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <% if logged_in? %>
    <div class="navbar-nav">
      <%= link_to "Dashboard", dashboard_path, class: "nav-item nav-link active" %>
      <%= link_to "Profile", player_path(current_user), class: "nav-item nav-link active" %>
      <%= link_to "Players", players_path, class: "nav-item nav-link active" %>
      <%= link_to "Logout", logout_path, method: :delete, class: "btn btn-secondary" %>
    </div>
    <% end %>
  </div>
</nav>