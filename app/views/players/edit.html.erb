<% provide(:button_text, 'Submit changes') %>
<h1 class='display-3'>Update your information</h1>
<p><%= link_to "#{status_button(@player)}", status_path(@player), class: 'btn btn-primary' %>
<h3>Edit your profile</h3>
<%= render 'form' %>